#include <stdio.h>
#include <conio.h>

int main() {
  /*
    printf(); // değeri okur
    scanf(); // değer alır
    puts(); // printf ile aynı görevde. Tek farkı alt satıra geçer.Tek parametre alır
    gets(); // scanf ile aynı sadece string alır. Tek parametre alır farkı boşlukdan sonra da alır
  */

  // int i;
  // printf("Bir deger giriniz: ");
  // scanf("%d", &i);
  // printf("Girilen Deger: %d", i);

  // puts("Merhaba Dunya");
  // puts("Merhaba Dunya");
  // puts("Merhaba Dunya %d", 11);

  // char x[40]; // sadece string
  // gets(x);
  // printf("Girilen Deger: %s", &x);

  return 0;
}
